import {ThemeSwitch} from '@/components/shared/theme-switch'
import {Container} from '@/components/ui/container'

export const Header = () => {
  return (
    <Container>
      <ThemeSwitch />
    </Container>
  )
}
